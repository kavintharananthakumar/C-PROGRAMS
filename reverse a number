#include <stdio.h>
int main(){
    int a,rev=0,n;
    printf("enter a number");
    scanf("%d",&n);
    while (n!=0)
    {
        a=n%10;
        rev=rev*10+a;
        n/=10;
    }
printf("the reverse is:%d",rev);
return 0;
}
