#include <stdio.h>
int main(){
    int a,s=0,n;
    printf("enter a number");
    scanf("%d",&n);
    while (n!=0)
    {
        a=n%10;
        s=s+a;
        n/=10;
    }
printf("the sum of the digits is:%d",s);
return 0;
}
