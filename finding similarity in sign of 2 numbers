#include <stdio.h>

int main() {
    int a, b;

    printf("Enter a, b: ");
    scanf("%f %f", &a, &b);
    if (((a>=0) && (b>=0))||((a<0)&&(b<0)))
    {
        printf("same sign");
    }
    else
    {
        printf("different sign");
    }
    
    return 0;
}
